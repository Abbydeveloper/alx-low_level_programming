#include <ctype.h>
#include <stdio.h>

/**
* main - Prints the alphabet in lower and uppercase using putchar
*
* Return: Always 0 (Success)
*/

int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
