#include <stdio.h>

/**
 * main - Print lower case alphabets
 *
 * Return: 0 if successful
 */

int main(void)
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z' ; alpha++)
		putchar(alpha);
	putchar("\n);
	return (0)
}
