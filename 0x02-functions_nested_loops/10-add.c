#include <stdio.h>
#include "main.h"

/**
 * add - add two integers and returns the result
 *
 * @c: first integer
 * @d: second integer
 *
 * Return: the result of the sum of the two integers
 */

int add(int c, int d)
{
	int i;

	i = c + d;
	return (i);
}
